# TinyVulkanRenderer

## 项目简介
当前项目是一个基于Vulkan的3D渲染引擎，实现了着色器变体，RenderGraph等引擎常用功能

./engine目录下是引擎的核心渲染逻辑，该模块应当被设计为独立于窗口系统，可以以dll的形式作为一个通用的渲染器使用

./app目录下是一个基于imgui的简易窗口系统，搭配有一些简易的交互逻辑，用于引擎的功能展示

./src/main.cpp文件是整个项目程序的入口

当前引擎支持gltf格式的场景读取，测试用场景文件存储在./scenes目录下


## 项目规则
1. 当前项目基于cmake构建，测试时请使用./run.ps1脚本一键编译运行，可以在运行时添加--debug参数以查看调试信息

2. 在我没用明确说明允许修改源码时，请勿修改任何项目代码，请先给我列出修改计划，得到我的明确允许后才能进行代码修改

3. 在遇到难以解决的bug时，可以在代码中添加调试埋点以获取详细运行情况，添加的埋点请以如下格式编写，方便后续定位埋点位置以及删除埋点，其中debug-description和debug-output根据实际情况替换:
```cpp
// DEBUG_BEGIN ...
std::cout << "[debug-description] debug-output" << std::endl;
// DEBUG_END
```

4. ./docs目录下存储有项目相关文档，后续的方案文档导出和读取请在此目录下进行

5. 在处理gltf文件读取功能时，请参考./docs/gltf目录下的gltf官方文档，了解该场景结构的约定，不能仅凭借印象推测

6. 遇到不确定的函数或api功能时，请查阅Vulkan官方Api文档或相关库源码，千万不要仅凭印象猜测
